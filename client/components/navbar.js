import React from 'react';
import { Menu } from 'semantic-ui-react';

const Navbar = () => (
	<div>
		<Menu stackable size="massive">
			<Menu.Item name="GarbGuess" icon="rain" />
		</Menu>
	</div>
);

export default Navbar;
